<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="#7f8c8d"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".SubmitActivity"
    >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"
        android:layout_marginTop="70dp"
        android:id="@+id/ctWrapper"
        android:tag="ctWrapper"
        >
        <Spinner
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/spinner"
            android:layout_below="@+id/ctWrapper"
            android:entries="@array/clients_array"
            android:background="@drawable/ri_blue"
            android:layout_alignParentStart="true" />
        <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/comment"
            android:inputType="textMultiLine"
            android:background="@drawable/ri_blue"
            android:textSize="20dp"
            android:textColor="#ffffff"
            android:height="100dp"
            android:layout_marginTop="10dp"
            android:padding="10dp"
            />
        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Submit"
            android:id="@+id/submitButton"
            android:layout_below="@+id/ctWrapper"
            android:background="@drawable/ri_red"
            android:layout_marginTop="10dp"
            android:textColor="#ffffff"
            android:textSize="20dp"
            android:layout_alignParentStart="true" />
    </LinearLayout>






</RelativeLayout>
